# AIGC 使用记录

> 本文件按本项目真实的开发过程整理。提交前请对照自己的操作核对一遍，
> 尤其是「人工修改」一栏，请改成你实际动手做过的事。

工具说明：主力工具为**豆包**（需求拆解、方案设计与逐阶段代码），
开发后期用**DeepSeek**处理关卡生成器的密度调优与测试补全。

## 汇总表

| # | 子任务 | 工具 | 提出的要求 | AI 完成内容 | 实际效果 | 人工修改 |
|---|---|---|---|---|---|---|
| 1 | 环境搭建 | 豆包 | 给出项目骨架与首次提交步骤 | 目录结构、`.gitignore`、`requirements.txt`、README 初稿、环境冒烟 `main.py` | `pip install pygame` 在 Python 3.14 上源码编译失败 | 排查后改用 `pygame-ce 2.5.8`，`requirements.txt` 与 README 都补上原因说明 |
| 2 | 设计草稿与数据结构 | 豆包 | 写 `docs/design.md`，并给出方向枚举、网格、关卡、棋盘类 | 状态机表、数据结构定义、路径检测伪代码、三关布局 | 三关手写布局都能通关 | 亲手验收每条通关顺序；后续把「箭头格子」升级为「可拐弯的折线箭」 |
| 3 | 路径检测 `can_fly` | 豆包 | 四方向检测，遇箭返回 False，出界返回 True，不能越界 | `can_fly` 方法 + 17 个 pytest 用例 | 一次跑通 17 passed | 核对四方向增量；补上「折线身体不横扫」的专项用例 |
| 4 | 界面框架与控件 | 豆包 | 钮标控件、开始界面、状态切换 | `Button` / 图标按钮、状态机界面骨架 | 界面能跑，但快捷键无响应 | 定位到事件分发没接上，改成统一的 `_dispatch_event` |
| 5 | 箭头造型与动画 | 豆包 | 箭头美化、飞出加速淡出、被挡红闪 + 晃动 | 多边形箭头、飞出 / 弹回动画 | 有毛刺、被挡反馈不够明显 | 改成「逐段粗线 + 节点圆」的圆角折线；被挡改为冲一下再弹回 + 暗红常驻标记 |
| 6 | 关卡生成器 | 豆包 → DeepSeek | 用逆向构造法批量生成必然可通关的关卡 | `generator.py`：代价优先放置 + 身体随机生长 + 通关校验 | 初版填充率只有 0.53 ~ 0.86，盘面明显比参考图稀疏 | 把 `straight_bias` 从 0.62 降到 0.28、`max_piece` 提到 20，填充率升到 0.88 ~ 1.00 |
| 7 | 玩法扩展 | DeepSeek | 求解器、提示、撤销、星级、选关、随机关、存档、音效 | `solver.py` / `storage.py` / `audio.py` 等 8 项扩展 | 全部接入主流程，63 个测试全绿 | 提示与自动通关共用同一求解器；随机关卡不写进存档进度 |
| 8 | 日夜主题与界面还原 | DeepSeek | 对照参考图与录屏还原顶栏 / 底栏，并做日夜两套配色 | `theme.py`、`icons.py`、`hud.py`；箭头配色按主题解析 | 夜间主题与参考图基本一致 | 日间配色压深一档避免浅底发灰；拨杆改为直接读主题，消除状态不同步 |
| 9 | 测试与文档 | DeepSeek | 补单元测试与端到端测试，更新 README / 设计文档 / 测试记录 | `tests/` 三个新文件共 63 个用例；三份文档 | 无窗口跑通，1.8 s | 删掉调试脚本与过期截图；截图脚本改用临时存档，避免污染真实进度 |

---

## 详述：三条值得单独讲的记录

### 记录 A：环境问题 —— 为什么用 `pygame-ce` 而不是 `pygame`

- **工具**：豆包
- **提出的要求**：搭好项目骨架，`main.py` 里放一个能弹出窗口的环境冒烟测试。
- **AI 完成内容**：给出目录结构、`.gitignore`、`requirements.txt`、README 初稿和冒烟脚本。
- **实际效果**：`pip install pygame` 在 Python 3.14 上尝试本地源码编译，报
  `No module named 'setuptools._distutils.msvccompiler'`。原因是 Python 3.14 刚发布，
  官方 pygame 还没有对应的预编译 wheel。
- **人工修改**：换成社区版 `pygame-ce`（API 与 pygame 2.x 兼容，导入名仍然是 `pygame`），
  并在 `requirements.txt` 和 README 里写明这个坑，免得换台机器又踩一次。

### 记录 B：从「箭头格子」到「可拐弯的折线箭」

- **工具**：豆包 → DeepSeek
- **提出的要求**：做出的效果要和参考小游戏一样 —— 盘面是密密麻麻的彩色折线，点一下整条线滑出去。
- **AI 完成内容**：把数据结构从「一格一个方向字符」改成 `{"cells": [...], "dir": ...}`；
  绘制改成「相邻节点之间画粗直线 + 每个节点画一个圆」，这样拐角是圆顺的，拐弯处不会出现缺口；
  飞行时整条线段预渲染成一张 Surface，沿「自身格子 + 箭头延长线」的路径平移。
- **实际效果**：一次跑通，但第一版的箭头又粗又直，和参考图差距明显。
- **人工修改**：
  - 线宽比例 `SEGMENT_WIDTH_RATIO` 从 0.36 调到 0.26，让格子和点阵能透出来；
  - 明确「判定只看箭头那一条射线，身体不参与碰撞」，并补了 `test_bent_flows_along_own_path` 锁住这条规则。

### 记录 C：关卡太稀疏 —— 生成器的密度调优

- **工具**：DeepSeek
- **提出的要求**：参考录屏里的盘面几乎是铺满的，我生成的关卡只有一半覆盖率，看不出是同一个游戏。
- **AI 完成内容**：保留逆向构造法的主体，把「单支箭的生长策略」参数化（`max_piece` /
  `straight_bias` / `stop_chance` / `window`），然后扫参数对比填充率。
- **实际效果**：扫出两条相互矛盾的规律 ——
  - 线段越短，盘面越碎但空隙越多（`max_piece=6` 时填充率只有 0.66 ~ 0.77）；
  - 线段越长，铺得越满但容易变成几根长横条，不像参考图。
- **人工修改**：最终取 `max_piece=20 + straight_bias=0.28`：箭很长，但七成以上的步骤都在拐弯，
  于是既铺得满（0.88 ~ 1.00）又保持「一堆绕来绕去的折线」的观感；
  同时把每关的随机重启次数提到 200 次取最优，压掉大造型的方差。

---

## 复查提醒（助教可能问到的点）

- `can_fly_arrow` 为什么只看箭头射线、不需要检查身体路径？（因为身体沿自身折线滑出，不横扫）
- 逆向构造法的两条约束分别排除什么？（排除「自己射线上已有箭」和「自己压在别人射线上」）
- 为什么生成出来的关卡开局每支箭都能飞？（这是算法的副产品，顺序错了才会出现飞不出去的箭）
- `solve()` 的 `failed` 集合是干什么的？（同一局面失败一次就记住，避免重复搜索）
