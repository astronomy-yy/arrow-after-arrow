# AIGC 使用记录

> 本文件按本项目真实的开发过程整理。提交前请对照自己的操作核对一遍，
> 尤其是「人工修改」一栏，请改成你实际动手做过的事。

工具说明：主力工具为**豆包**（需求拆解、方案设计与逐阶段代码），
开发后期用**DeepSeek**处理关卡生成器的密度与难度调优、求解器重写与测试补全。

## 汇总表

| # | 子任务 | 工具 | 提出的要求 | AI 完成内容 | 实际效果 | 人工修改 |
|---|---|---|---|---|---|---|
| 1 | 环境搭建 | 豆包 | 给出项目骨架与首次提交步骤 | 目录结构、`.gitignore`、`requirements.txt`、README 初稿、环境冒烟 `main.py` | `pip install pygame` 在 Python 3.14 上源码编译失败 | 排查后改用 `pygame-ce 2.5.8`，`requirements.txt` 与 README 都补上原因说明 |
| 2 | 设计草稿与数据结构 | 豆包 | 写 `docs/design.md`，并给出方向枚举、网格、关卡、棋盘类 | 状态机表、数据结构定义、路径检测伪代码、三关布局 | 三关手写布局都能通关 | 亲手验收每条通关顺序；后续把「箭头格子」升级为「可拐弯的折线箭」 |
| 3 | 路径检测 `can_fly` | 豆包 | 四方向检测，遇箭返回 False，出界返回 True，不能越界 | `can_fly` 方法 + 17 个 pytest 用例 | 一次跑通 17 passed | 核对四方向增量；补上「折线身体不横扫」的专项用例 |
| 4 | 界面框架与控件 | 豆包 | 钮标控件、开始界面、状态切换 | `Button` / 图标按钮、状态机界面骨架 | 界面能跑，但快捷键无响应 | 定位到事件分发没接上，改成统一的 `_dispatch_event` |
| 5 | 箭头造型与动画 | 豆包 | 箭头美化、飞出加速淡出、被挡红闪 + 晃动 | 多边形箭头、飞出 / 弹回动画 | 有毛刺、被挡反馈不够明显 | 改成「逐段粗线 + 节点圆」的圆角折线；被挡改为冲一下再弹回 + 暗红常驻标记 |
| 6 | 关卡生成器 | 豆包 → DeepSeek | 用逆向构造法批量生成必然可通关的关卡 | `generator.py`：代价优先放置 + 身体随机生长 + 通关校验 | 初版填充率只有 0.53 ~ 0.86，盘面明显比参考图稀疏 | 把 `straight_bias` 从 0.62 降到 0.28、`max_piece` 提到 20，填充率升到 0.88 ~ 1.00（密度达标，但**引入了「没有阻挡」的缺陷**，见第 11 条） |
| 7 | 玩法扩展 | DeepSeek | 求解器、提示、撤销、星级、选关、随机关、存档、音效 | `solver.py` / `storage.py` / `audio.py` 等 8 项扩展 | 全部接入主流程，63 个测试全绿 | 提示与自动通关共用同一求解器；随机关卡不写进存档进度 |
| 8 | 日夜主题与界面还原 | DeepSeek | 对照参考图与录屏还原顶栏 / 底栏，并做日夜两套配色 | `theme.py`、`icons.py`、`hud.py`；箭头配色按主题解析 | 夜间主题与参考图基本一致 | 日间配色压深一档避免浅底发灰；拨杆改为直接读主题，消除状态不同步 |
| 9 | 测试与文档 | DeepSeek | 补单元测试与端到端测试，更新 README / 设计文档 / 测试记录 | `tests/` 三个新文件共 63 个用例；三份文档 | 无窗口跑通，1.8 s | 删掉调试脚本与过期截图；截图脚本改用临时存档，避免污染真实进度 |
| 10 | 修复窗口拖不动 / 缩不了 | DeepSeek | 定位「无法调整窗口大小、也拖不动」的原因并修复 | 用 Win32 API 探出窗口外框在 `top=-81 / bottom=958`，判定为窗口高过桌面 + 非 DPI 感知；补 `desktop_size()` / `fit_to_screen()`、重构 `_resize()`、新增 `tests/test_window.py` 12 个用例 | 窗口上下边框回到屏幕内（`39 / 838`），75 个测试全绿 | 实机确认边框可抓取后才提交；界面提示文字与文档同步更新 |
| 11 | 修复「关卡没有阻挡、无脑必通关」 | DeepSeek | 玩家反馈「关卡设计得互相没有阻挡，直接是必通关」，要求做成真的需要排序才能过 | 定位到生成器第二条约束（身体不许压在别人射线上）与第一条合起来等价于 `射线(a) ∩ 身体(b) = ∅`，数学上排除了任何阻挡；改为单约束 + 新增 `ray_pref` 难度旋钮，12 关全部重新生成并配出递减难度曲线 | 开局能直接点的箭从 12 关全部 100% 变为 67% → 11% 逐关递减；被挡比例 33% → 89%；填充率反而从 0.92 升到 0.94 ~ 1.00 | 先写诊断脚本量出「100% 可飞、0% 被挡」才动手改；把当初锁住这个 bug 的测试改成反向断言 |
| 12 | 求解器重写为拓扑排序 | DeepSeek | 关卡有阻挡后，提示与 AI 自动通关开始报「这一步好像卡住了」 | 发现原 DFS + 记忆化在平均四五支可飞、深度二十层的盘面上会指数爆炸，60 万节点上限打满，**把有解的关卡判成无解**；改为「谁挡谁」建图 + Kahn 拓扑排序，O(n²) 精确求解，有环才判无解 | 12 关全部毫秒级求解成功；新增 `tests/test_blocking.py` 覆盖阻挡链 / 环路 / 性能 | 补「求解器入度为 0 的集合必须与棋盘可飞集合完全一致」的交叉验证测试 |
| 13 | 修复飞出动画不平滑（转弯处抖动） | DeepSeek | 玩家反馈「线飞出去的时候不够平滑，特别是转弯处」 | 定位到飞出采样只按整格取点，落在两采样点之间的拐角被连成一条斜弦切掉，且随相位每秒「削平复原」26 次；改为取折线真正的一段（`flow_points`，长度守恒），圆点与箭头改用 4 倍超采样贴图做抗锯齿，描边只在拐点与两端补圆点，单帧步进加 50 ms 上限 | 转弯处不再抖动，线在飞行中厚度与长度恒定；新增 `tests/test_motion.py` 22 个用例，全量 150 个用例通过，整帧 4.3 ms | 先量化（采样点、折线长度、像素厚度）再动手；否掉「整幅画面超采样」改用两处小贴图；发现 `.git` 对象库损坏后从远端恢复仓库并关掉 `gc.auto` |
| 14 | 箭头与线段美化 | DeepSeek | 玩家反馈「箭头和线再美化一下」 | 先把参考录屏用 `imageio_ffmpeg` 抽帧、numpy/PIL 逐像素量成数字（金黄那支箭实占 14 px、格心间距约 56 px → 线宽定为格距的 0.30，上限 12→15 px）；箭头由等腰三角改成 4 顶点「飞镖」（倒钩前掠 0.42 倍线宽、后缘内凹兜住线身圆头）；整支箭加一圈等厚外描边（线身 `+2×rim`、箭头 `pad=rim`，描边色 = 线身色乘系数）；顺带查出「拐点/端头鼓 1 像素」的根因是圆点直径取成了 `2r+1` | 线宽与箭头造型对齐参考图，拐点与端头不再鼓包；新增 `tests/test_stroke.py` 43 个用例，全量 193 个用例通过 | 先量数字再动手；否掉「把整支箭放大一圈」当描边（会让箭头粗出好几倍）；把两个坑写回 skill 备查 |
| 15 | 界面 / 背景 / 按钮美化 | DeepSeek | 玩家反馈「界面、背景、按钮等再美化一下」 | 新增 `game/paint.py`：竖向渐变、径向柔光、圆角投影、卡片面（渐变+圆角+细描边+顶部高光+投影）、文字阴影，全部按参数预渲染并缓存；`theme.py` 补 17 个层次配色字段（日夜各一套）；背景改渐变+柔光，棋盘垫圆角面板，顶底栏改半透明玻璃+渐隐阴影，按钮/卡片/菜单项/滑杆全部改渐变+描边+投影，按钮悬停提亮描边、按下下沉 2 像素 | 界面有了层次，不再是「纯色底 + 硬描边」；新增 `tests/test_paint.py` 34 个用例，全量 227 个用例通过；`tools/perf_bench.py` 量得各界面整帧 2.45~5.72 ms，无回退 | 顺手修掉三处排版事故（开始页文字压在卡片外、选关页下半屏大片空白、菜单最后一项顶在面板底边上）；新增离屏截图 `settings.png` / `level_clear_day.png` 逐张人工核对 |
| 16 | 开始页四个入口与字母玩法 | DeepSeek | 玩家要求美化开始页：标题改艺术字，下面放「规则介绍 / 基础玩法 / 字母玩法 / 随机关卡」四个按钮；规则页要写明按键（含 `U` 撤销）但**去掉 `N` 是随机关卡**这一条；字母玩法共 26 关、每关盘面拼成对应的大写字母；随机关从 `N` 键搬到按钮上 | `game/paint.py` 新增 `art_text`（3 倍超采样 + 圆盘偏移做等厚描边 + 按 alpha 乘渐变/高光 + 投影，无需额外字体）；`game/letters.py` 内置 26 个 5×7 点阵字模并光栅化成棋盘遮罩，字母关连通性改用**八连通**；`tools/generate_letters.py` 生成 26 关（难度沿字母序递进）；`game/states.py` 扩到 8 个状态，`main.py` 用 `self.track` 区分基础 / 字母两条线；`icons.py` 补四个入口图标、`ui.Button` 支持 `icon`；删掉 `K_n` 分支与规则页对应的那一行 | 开始页变成「艺术字标题 + 四个入口 + 进度统计」；四条线各有独立入口且互不干扰；`N` 键彻底移除。新增 `tests/test_letters.py` 104 个用例，全量 332 个用例通过；新增界面整帧 1.70~6.66 ms | 先量了「放大后横向溢出 131~171 px」才修拖不动（上一轮）；本轮先写探针脚本确认 26 个字母遮罩都连通、都能生成，才动手改主流程；专门补了一条 `board_size()` 必须是 `(行, 列)` 的用例，因为**这个顺序写反过一次**，直接害得 C/G/J/U/V 五个字母生成失败 |
| 17 | 顶栏靶心按钮回错选关页（回归） | DeepSeek | 玩家反馈「点上面那个圆圈退出时老是退到基础关卡选择界面」 | 查出这枚顶栏按钮接的是 `open_level_select` —— 一个在**类定义时**用 `open_level_select = open_basic_select` 绑死到基础选关的别名，于是玩字母关时点它也掉进基础选关页；回调改接 `open_select`（按 `self.track` **现场分派**），并把这个别名直接删掉 | 字母关 / 基础关各回各的选关页，随机关回基础选关页；新增 3 个回归用例（走完整点击链路：点击 → HUD → IconButton → 回调），全量 335 个用例通过 | 这类「别名在类定义时绑死」的回调**不报错、只走错分支**，光读代码发现不了；回归用例必须真的发一个点击事件走完整链路，只测那个方法测不出来 |
| 18 | 棋盘格子数加密到约 1.4 倍 | DeepSeek | 玩家要求「棋盘格子数增加」 | 先按 640×1140 画布实算三档密度再让用户选（格子 44→31→24 px、线宽 13→9→7 px），选了中间的 1.4 倍档；字母关 `DEFAULT_SCALE` 2→3（16×12 → **23×17**，391 格），基础关尺寸整体放大（12×9 → 17×13，最大到 25×19），随机关尺寸表同步放大；`MAX_PIECE` 14→20（笔画变宽，一支箭可以吃更长） | 棋盘更精细、字母弧线明显平滑；箭数 15.9 → 29.0（平均）；全量 338 个用例通过；最贵一档整帧 3.32 → 6.02 ms，仍在 16.7 ms 预算的三分之一；放大到最大时 12 关全部不横向溢出 | 加密本身没动几何计算（格子按可视区反算，改数据即可），但把**生成参数**带偏了：难度曲线出现小反转 —— 每关都在自己上限内独立挑「最满」的一版，实际值可能反而比上一关高。修法是上限跟着上一关**实际值**往下压，且压不动时**换种子重试**，并按 `(是否满足上限, 填充率)` 取最优（只压难度会让填充率掉到 0.887） |
| 19 | 顶栏靶心按钮改「返回来处」 | DeepSeek | 玩家反馈「随机关卡，点圆圈没有退回到首页，又退回到基础关卡选择页面」 | 第 17 行只把它改成「按 `self.track` 分派」，而随机关卡借用了 basic 这条线，于是仍被当成基础关塞进基础选关页。这次改成认**来路**：`main.py` 加 `self.play_origin`，三个入口各自登记（基础选关页 / 字母选关页 / 开始页），靶心改接新的 `leave_level()` 按来路分派 | 基础 → 基础选关页、字母 → 字母选关页、随机 → **开始页**；新增 2 条回归用例（含「玩过随机之后再进基础关，来路不会被上一次污染」），改 1 条；全量 344 个用例通过 | 「按当前模式分派」对**不属于任何模式的入口**天然失灵 —— 随机关没有自己的选关页，只能借用别人的，于是永远回错。凡是「返回」语义，记来路比猜模式可靠；第 17 行只修了「回错哪一页」，没问「它本来该回哪一页」 |
| 20 | 标题改成卡通贴纸风拼装艺术字 | DeepSeek | 玩家给了一张参考截图：「开始界面的艺术字我想要截图这里面类似的」（奶白字面 + 粗黑描边 + 浅色外圈，两个「一」是红 / 蓝圆头横杠，末尾「箭」换绿色） | `art_text` 新增 `outline2` / `outline2_width`（外圈铺大圆盘、内圈盖上去，只留一圈宽）与 `lift`（描边整体下移、字面不动 → 底部边更厚，字浮在边上）；新增 `art_bar`（圆头胶囊横条，`pad` 算法与 `art_text` 逐项对齐）与 `art_banner`（横向按**墨迹边界**排布，避免每片四周的描边留白变成片间距）；`theme` 补 5 个配色字段，日夜各一套 | 标题从「一段渐变文字」变成五片拼装（红条 / 箭 / 又 / 蓝条 / 绿箭），占屏宽 79%；全量 344 个用例通过；开始页整帧 2.90 ms | 中间挖出两个坑：① 描边宽度一直用的是「超采样像素」而 `pad` 是「原始像素」，两边没统一，标称 6 px 的描边实际只有 2 px、外圈细到缩回后被平均成灰色；② 单位统一后圆盘半径变 18，逐点 blit 一次 42 ms、整页首帧 210 ms，改在 1/2 分辨率做膨胀再放大回来，首帧降到 62 ms |
| 21 | 规则页「怎么玩」文字排布 | DeepSeek | 玩家反馈「玩法规则这一页怎么玩这一栏文字排布不对」 | 根因是正文**写死一行**：26 字 × 23 px = 598 px，而卡片内宽只有 540 px，句子直接画到卡片外面（不报错、不抛异常、测试也照样过）；按键表右列的「缩放棋盘（也可用 - 与 =）」同样超出 8 px。新增 `paint.wrap_text()`（切单元 → 贪心 → **二分有效宽度做均衡** → 避头尾），规则页改成 `_compute_rules_layout()` **按内容累加**：先折行拿到正文行数，再据此算每块面板的高度，结果算一次缓存 | 三句话各折成两行且宽度摊匀（322/299、299/299、299/276），标点不落行首，全角括号整段不拆；三块面板依次下移、返回按钮跟着走；新增 8 条用例（含「量文字实际渲染宽度」的核心回归），全量 352 个用例通过 | 纯贪心折出的是「满满一行 + 两三个字的尾巴」（`…方向飞出` / `…颗红心` / `…失败`），很难看 —— 均衡折行才是对的做法；这类溢出**不会报错**，只能靠「量渲染宽度」的测试兜住 |
| 22 | 背景柔雾 + 按键配色 | DeepSeek | 玩家要求「开始界面的背景换成跟截图里面类似的（但要有一定改动），按键配色也是」 | 先逐像素采样参考图，确认它用的就是本项目的**日间**配色（按钮蓝 `117,166,243 → 89,140,228`、文字 `42,46,70`、进度条 `38,176,164` 全部对得上），于是方向定为「同一套结构，夜间换深色底」。新增 `Palette.fog`（四团错开的柔雾，**只在非对局界面**铺 —— 对局中会压住棋盘）、`paint.fog_layer()` 预合成成一张整屏贴图；新增 `Palette.ghost_*`，次要按钮从「深底白字」改成**浅色纸片 + 深字**（夜间的纸片是浅蓝白，纯白会晃眼） | 菜单类界面有了雾面层次，开始页按钮是「蓝 / 白 / 蓝 / 白」的节奏；柔雾让开始页 2.90 → 4.15 ms，预合成之后回到 3.38 ms；新增 2 条用例，全量 352 个通过 | 日间的雾是**压暗**而不是提亮（浅底叠淡蓝会降亮度），断言不能假定方向；旧的「次按钮比主按钮暗」那条断言的前提随设计反转一起作废，改成量「纸片比蓝底亮 / 蓝底明显偏蓝 / 纸片上是深字」 |
| 23 | 开始页暗纹底 + 卡通箭吉祥物 + 按键描边 | DeepSeek | 玩家给了一张开始页设计稿（薄荷青底 + 箭形暗纹 + 标题下方一只带脸的蓝色卡通箭），要求「标题下方那个箭」「背景换成跟截图里面类似的（但要有一定改动）」「按键配色也是」 | 新增 `paint.pattern_layer()` / `blit_pattern()`：底色渐变 + 平铺的同色箭形暗纹合成成**一张不透明贴图**，图案复用游戏自己的 `_arrow_shape`（隔行错开半格、奇偶格上下翻转），`Palette` 补 `menu_top/menu_bottom/pattern/pattern_alpha`；新增 `paint.arrow_mascot()`：投影 / 外描边 / 渐变箭身 / 贴边暗面 / 高光块 + 四角星 / 一张脸（眼白、瞳孔、高光点、两道眉毛、一道笑），整体倾斜 10°，`Palette` 补 7 个 `mascot_*` 字段；主按钮改成参考图那种「亮蓝填充 + **比填充更深**的描边」（`btn_line` 从「比填充亮」翻转）；开始页整体重排给吉祥物让位 | 开始页变成「薄荷青（日间）/ 深蓝（夜间）+ 同色箭形暗纹 + 贴纸标题 + 卡通箭吉祥物 + 蓝白交替按钮」，占屏宽 49% 的吉祥物夹在副标题与按钮之间互不重叠；新增 2 条用例、改 3 条，全量 354 个通过；开始页中位 **3.02 ms**（比上一版 3.38 还低，因为底整块预合成了） | 第一版画出来完全不像箭，连挖三个坑：① 圆角靠「在顶点画圆」，圆鼓到边的外侧，剪影成了**一根骨头** → 改成正规圆角多边形（角平分线内缩 r/tan(θ/2) 得切点、圆心距两边为 r，切点还得按绕行顺序连，否则连成蝴蝶结）；② 渐变只铺在「箭身框」内，箭尾圆头伸到框外被 `BLEND_RGBA_MULT` 乘成透明、**露出深色描边**（看着像尾巴被烧黑）→ 渐变铺满画布；③ 五官直接画在画布上会**浮在箭身外面** → 五官单独成层再用剪影 `BLEND_RGBA_MULT` 裁一刀 |

---

## 详述：十一条值得单独讲的记录

### 记录 A：环境问题 —— 为什么用 `pygame-ce` 而不是 `pygame`

- **工具**：豆包
- **提出的要求**：搭好项目骨架，`main.py` 里放一个能弹出窗口的环境冒烟测试。
- **AI 完成内容**：给出目录结构、`.gitignore`、`requirements.txt`、README 初稿和冒烟脚本。
- **实际效果**：`pip install pygame` 在 Python 3.14 上尝试本地源码编译，报
  `No module named 'setuptools._distutils.msvccompiler'`。原因是 Python 3.14 刚发布，
  官方 pygame 还没有对应的预编译 wheel。
- **人工修改**：换成社区版 `pygame-ce`（API 与 pygame 2.x 兼容，导入名仍然是 `pygame`），
  并在 `requirements.txt` 和 README 里写明这个坑，免得换台机器又踩一次。

### 记录 B：从「箭头格子」到「可拐弯的折线箭」

- **工具**：豆包 → DeepSeek
- **提出的要求**：做出的效果要和参考小游戏一样 —— 盘面是密密麻麻的彩色折线，点一下整条线滑出去。
- **AI 完成内容**：把数据结构从「一格一个方向字符」改成 `{"cells": [...], "dir": ...}`；
  绘制改成「相邻节点之间画粗直线 + 每个节点画一个圆」，这样拐角是圆顺的，拐弯处不会出现缺口；
  飞行时整条线段预渲染成一张 Surface，沿「自身格子 + 箭头延长线」的路径平移。
- **实际效果**：一次跑通，但第一版的箭头又粗又直，和参考图差距明显。
- **人工修改**：
  - 线宽比例 `SEGMENT_WIDTH_RATIO` 从 0.36 调到 0.26，让格子和点阵能透出来
    （后来在记录 H 里按参考录屏逐像素量了一遍，又提到 0.30）；
  - 明确「判定只看箭头那一条射线，身体不参与碰撞」，并补了 `test_bent_flows_along_own_path` 锁住这条规则。

### 记录 C：关卡太稀疏 —— 生成器的密度调优

- **工具**：DeepSeek
- **提出的要求**：参考录屏里的盘面几乎是铺满的，我生成的关卡只有一半覆盖率，看不出是同一个游戏。
- **AI 完成内容**：保留逆向构造法的主体，把「单支箭的生长策略」参数化（`max_piece` /
  `straight_bias` / `stop_chance` / `window`），然后扫参数对比填充率。
- **实际效果**：扫出两条相互矛盾的规律 ——
  - 线段越短，盘面越碎但空隙越多（`max_piece=6` 时填充率只有 0.66 ~ 0.77）；
  - 线段越长，铺得越满但容易变成几根长横条，不像参考图。
- **人工修改**：最终取 `max_piece=26 + straight_bias=0.24`：箭很长，但七成以上的步骤都在拐弯，
  于是既铺得满又保持「一堆绕来绕去的折线」的观感；
  同时把每关的随机重启次数提到 200 次（后来提到 240 次）取最优，压掉大造型的方差。
- **后续**：这一版为了「撑开走廊」多加了第二条放置约束，导致关卡完全没有阻挡
  （密度达标但玩法被抹掉），在记录 E 里修掉并重做了难度曲线。

### 记录 D：窗口拖不动、也缩不了 —— 一个只在特定屏幕上出现的缺陷

- **工具**：DeepSeek
- **提出的要求**：拖窗口边框没反应，窗口也拖不走，要求定位原因并修复。
- **AI 完成内容**：先排除「代码没处理缩放事件」这个方向 —— 用真实窗口 + Win32
  `MoveWindow` 做端到端验证，证明 `VIDEORESIZE` 确实会触发、`view_rect` 也会跟着更新。
  真正的原因换了个角度才看出来：窗口默认高 1000，而进程不是 DPI 感知的，
  在缩放 200% 的屏幕上 pygame 看到的桌面只有 `1440 x 900`，Windows 把窗口垂直
  居中后，实测窗口外框落在 `top = -81`、`bottom = 958` —— 标题栏和底边双双在
  屏幕之外，自然既抓不到边框、也抓不到标题栏。修复是加 `desktop_size()` 与
  `fit_to_screen()`，启动时按屏幕余量等比收敛（最小尺寸一起收敛），并顺手改掉
  `_resize()` 里多余的 `set_mode()`。
- **实际效果**：窗口外框变成 `top = 39 / bottom = 838`，上下边框回到屏幕内；
  75 个测试全绿，dummy 环境下的截图尺寸保持不变。
- **人工修改**：实机拖拽确认边框能抓住、能缩放之后才提交；
  同时更新 README 的操作表与设计文档 6.1 节。

### 记录 E：关卡没有阻挡 —— 一条「看起来在优化、实际在删玩法」的约束

- **工具**：DeepSeek
- **提出的要求**：玩家反馈「你这个关卡设计的，都互相没有阻挡，直接是必通关」。
- **AI 完成内容**：先写了个诊断脚本（对每一关统计开局能直接飞的箭、被挡箭的比例、
  平均每支箭被几支箭挡住），量出来的结果是 **12 关全部 100% 可飞、被挡比例 0%** ——
  玩家的反馈完全成立。根因在生成器的放置约束里：除了「自己的射线上不能有已放好的箭」
  （这条是可通关性的保证），还额外加了「自己的身体不能压在已放好箭的射线上」。
  两条合起来等价于对任意两支箭都有 `射线(a) ∩ 身体(b) = ∅`，也就是每支箭的出口
  走廊永远是空的，**阻挡在数学上不可能出现**，点什么都对。第二条约束对可通关性
  毫无贡献，是当初为了「撑开走廊让盘面好看」而加的，纯属把玩法删掉了。
- **实际效果**：删掉第二条约束后，后放的箭（先点）可以合法地站在先放的箭（后点）的
  射线上，阻挡自然出现。同时把「前方空走廊长度」做成难度旋钮 `ray_pref`：实测从 0 到 1，
  开局可飞比例 99% → 22%，而被挡比例 0.9% → 78%，**填充率反而从 0.924 升到 0.965**
  （走廊长意味着那些格子会被后面的箭填满，铺得满和有阻挡互相成全）。
  12 关重新生成后开局可飞比例逐关递减：67% → 11%。
- **人工修改**：
  - 先把诊断脚本跑出来确认结论，再动生成器，避免凭感觉改；
  - `tests/test_game_flow.py` 里原有一条 `test_generated_level_has_no_free_lunch_before_moving`，
    断言的正是「开局每支箭都能飞」—— 它把这个 bug 当成了预期行为锁住，改成反向断言
    「必须有能点的，也必须有点不动的」；
  - 新增 `tests/test_blocking.py`（29 个用例）把「有阻挡」和「难度递减」锁进回归测试。

---

### 记录 F：棋盘放大后拖不动 —— 以及「拖动」与「点击」抢同一个左键

- **工具**：DeepSeek
- **提出的要求**：玩家反馈「放大缩小棋盘，但是无法拖动棋盘啊」。
- **AI 完成内容**：先量化前提，写脚本量了 12 关在 `ZOOM_MIN` / 100% / `ZOOM_MAX`
  三档下的棋盘像素尺寸与可视区之差，结果是**最大缩放下每一关都横向溢出 131~171 像素**
  （第 7、12 关纵向还溢出 227 / 252 像素），溢出的部分玩家根本够不着。
  根因是 `_compute_geometry()` 只有「居中」一种摆法，没有平移量。修法是把棋盘几何拆成
  可用区（顶栏底栏之间）、零平移基准、边界夹取三层，加一个 `self.pan`；
  夹取结果立刻写回 `pan`，避免拖到边界后往回拖时先空转一段。
  同时把滚轮缩放做成**锚定指针**的（记下锚点在棋盘里的相对位置，缩放后反解 `pan`），
  否则「放大看角落」要放大一下再拖一下来回倒。
- **实际效果**：放大后按住棋盘即可拖动（中键 / 右键也能拖），滚轮缩放锚在鼠标上，
  方向键微调、`0` 复位；棋盘最多拖到边缘与可视区对齐，永远不会被拖出屏幕。
  新增 `tests/test_pan.py`（24 个用例），全量 128 个用例通过。
- **人工修改**：
  - **改判定而不是改操作**：最初想「按住就拖」，但想到正式关卡填充率 0.94~1.00、
    几乎每格都是线段，按下即点的话想拖棋盘就一定先点飞一支箭（被挡还白扣一颗心）。
    于是自己确定改成**按下记账、抬起了结**：位移不超过 `DRAG_THRESHOLD`(8 逻辑像素)
    才算点击，超过就改判为拖动、这次点击作废。这是对玩法手感影响最小的一种解法。
  - 因此 `tests/test_game_flow.py` 里的 `click_cell()` 从只发 `MOUSEBUTTONDOWN`
    改成成对发「按下 + 抬起」—— 不改的话端到端用例会全部失效（它们本来就没有松手事件）；
  - 把顶栏 / 底栏的控件事件**提前拦掉**（被控件吃掉的事件不再往下传给棋盘），
    否则放大后棋盘铺到工具栏底下时，点一个按钮会顺带飞走一支箭；
  - AI 给的 `reset_view()` 第一版写成 `on_zoom_slider(1.0)`，把滑杆比例 1.0 当成了
    100% 缩放（实际是 `ZOOM_MAX`），被自己的用例抓出来后改成直接写 `self.zoom = 1.0`；
  - 顺手把「缩放 + 拖动」的状态出一张离屏截图人工核对（`playing_zoom_pan.png`）。

---

### 记录 G：飞出动画不平滑 —— 拐角被斜弦切掉

- **工具**：DeepSeek
- **提出的要求**：玩家反馈「线飞出去的时候不够平滑，特别是转弯处」。
- **AI 完成内容**：先把「不平滑」拆成可量化的东西，写脚本把飞出动画的采样点逐帧打印出来，
  一眼看出问题：飞出是「各节沿自己那条折线往前流」，但采样只取
  `s = offset, offset+1, offset+2 …`（每格一个点），**落在两个采样点之间的拐角会被这两点
  连成一条斜弦切掉** —— 直角被削成斜角。而 `offset` 是连续增长的，所以这个斜角每前进一格
  就「削平 → 复原」一次，`FLY_SPEED = 26` 格/秒 就是每秒削 26 次，看起来正好是转弯处在抖。
  量化后：40 像素格距下被切掉的线长最多 **23 像素**，并且在 0~23 之间来回振荡
  （等于「线的长度在呼吸」）。
  修法是把区间内的整数节点补回来（`flow_points()`），画出来的就是原折线真正的一段；
  折线长度因此恒等于 `(节数-1) × 格距`，与相位无关。
  第二件事是抗锯齿：pygame 的 `draw.circle` / `draw.polygon` 都不带抗锯齿，圆点和箭头斜边
  是一排锯齿，动起来还会「爬行」。改成把圆点与箭头三角用 **4 倍超采样**画好再缩回来，
  并按「形状 + 尺寸 + 颜色」缓存（每帧只是几次 `blit`）。
- **实际效果**：转弯处不再抖动，飞行过程中线的厚度与折线长度都保持恒定；
  拐角变成原来的直角（只有外侧圆角），箭头斜边也不再是锯齿。
  新增 `tests/test_motion.py`（22 个用例），全量 150 个用例通过；整帧 4.3 ms，无性能回退。
- **人工修改**：
  - **先量化再动手**：把采样点、折线长度、渲染出的像素厚度都跑成数字，
    确认「削角 + 每秒 26 次削平复原」的结论后，才去改取段逻辑；
  - 抗锯齿**没有**用「整幅画面超采样」：那样每帧要新建一张上百万像素的 Surface 再缩回来
    （实测约 2.5 ms/帧，盘面大时会翻倍）。因为折线都是横平竖直的、直段的边本来就锐利，
    所以只把**圆点和箭头三角**这两处斜边/曲边做成带抗锯齿的小贴图，其余照旧，
    开销降到可以忽略；
  - AI 给的描边是「每个节点都补一个圆点」（沿用原来的写法），放大看发现圆点比线段宽
    1~2 像素，整条线上挂着一串小疙瘩、直线看着都不直。改成只在两端与真正拐弯的节点补
    （用叉积判断），并用像素厚度断言锁进测试；
  - 顺手给主循环的单帧步进加了 50 ms 上限（`_frame_dt()`）—— 切窗口回来时 `clock.tick()`
    可能一次返回几百毫秒，飞行线会一步跨过好几格，也是「不平滑」的一种；
  - 收尾时发现仓库的 `.git` 对象库损坏（pack 文件丢失、分支引用指向不存在的对象，
    `HEAD` 变成 unborn），好在**所有历史都已推到远端**：先备份整个工作区，再从
    `origin/main` 取回对象、重建 `main` 引用与索引，改动一条不丢；顺手关掉 `gc.auto` 以防复发。

---

### 记录 H：箭头和线再美化一下 —— 先把参考图量成数字

- **工具**：DeepSeek
- **提出的要求**：玩家反馈「箭头和线再美化一下」。
- **AI 完成内容**：
  1. **量参考图**：用 `imageio_ffmpeg` 内置的 ffmpeg 把参考录屏抽帧，再用 numpy + PIL
     逐像素量线宽与格子间距 —— 金黄那支箭实占 **14 px**，相邻格心间距约 **56 px**，
     线宽约为格距的 1/4。据此把 `SEGMENT_WIDTH_RATIO` 从 0.26 提到 0.30，
     `MAX_SEGMENT_WIDTH` 从 12 提到 15；
  2. **箭头改形**：参考图里的箭头后缘是往内兜的（把线身的圆头包进去），不是等腰三角。
     于是把箭头从 3 个顶点改成 **4 个顶点** 的飞镖 —— 尖端、两枚**前掠**的倒钩
     （`HEAD_SWEEP_RATIO = 0.42`）、尾根处的收口点，尺寸按线宽缩放
     （`HEAD_SPAN_RATIO = 1.90`、`HEAD_LENGTH_RATIO = 2.15`）；
  3. **加外描边**：深色底上线段的边缘本来就靠明度差「切」出来，两条线挨着时容易糊成
     一片。给整支箭加一圈外描边，颜色取「线身色 × 系数」而不是「线身色 − 常数」——
     乘系数色相不变，减常数会把蓝、紫这类深色压成黑；力度按主题给（夜间 0.46、日间 0.72）。
     描边的画法**不是**把整支箭放大（那样箭头会比线身多粗出好几倍），而是拿同一套函数
     画两遍：先画「线身加宽 `2×rim` + 箭头外撑 `rim`」的描边色，再画线身；
  4. **顺带查出老疙瘩的真正原因**：圆点贴图原来取直径 `2r+1`（画整数圆的老规矩），
     而 pygame 画粗线时涂的是 `y_center - (w-1)//2` 起的 `w` 行 —— 直径比线宽多 1 像素，
     于是每个拐点、每个端头都鼓出一小块。改成直径 `2r`，并把「宽 `w` 的笔画落在
     `center` 上从哪一行开始」固化成 `stroke_span()`，圆点、箭头贴图都按它摆。
- **实际效果**：线宽与箭头形态对齐参考录屏；线段边缘更利落，相邻线段之间多了一层分隔；
  拐点 / 端头的 1 像素鼓包消失；静态线段与飞行线段仍然逐像素一致。
  新增 `tests/test_stroke.py`（26 个函数 / 43 个用例），全量 **193 个用例**通过。
  整帧耗时无明显回退（第 1 关 3.88 ms，第 12 关 4.76 ms，关掉描边约 3.46 ms）。
- **人工修改**：
  - 没有凭感觉调参数：先用 ffmpeg 抽帧 + numpy 逐像素量出「14 px / 56 px」这个比例，
    才去改 `SEGMENT_WIDTH_RATIO`；
  - 否掉了「整体放大一圈就是描边」的省事写法 —— 放大对线身成立，但箭头那几倍的斜边
    会一起变粗，看着像箭头包了一圈毛巾；改成线身与箭头各按自己的外扩量画；
  - 描边一开始用「减去一个固定值」压暗，浅色（金黄、薄荷）看着还行，深色（蓝、紫）
    直接掉成黑边。改成乘系数并把力度抽成主题参数，日间改用 0.72；
  - 测试写完发现「线宽处处相等」这条断言老是偶发失败，排查后确认是自己量错了行
    （没处理偶数宽的半像素偏移、把拐点的并集当成了单行）—— 改成量整段像素带、
    再和「两臂并集」比对之后稳定通过。

---

### 记录 I：界面美化 —— 用「预渲染贴图 + 缓存」换层次感

- **工具**：DeepSeek
- **提出的要求**：玩家反馈「界面、背景、按钮等再美化一下」。
- **AI 完成内容**：
  1. **先判断「平」在哪儿**：把六张截图并排看了一遍，问题很清楚 —— 背景是一块纯色、
     没有任何明暗变化；按钮、卡片、菜单项都是「一块纯色 + 一圈硬描边」，跟背景糊在
     一起；按下去只是换个颜色，没有任何物理反馈。所以要做的是**层次**，而不是换配色；
  2. **单独的绘制模块**：新增 `game/paint.py`，提供五样原语 —— 竖向渐变、径向柔光、
     圆角投影、卡片面（渐变填充 + 圆角裁剪 + 细描边 + 顶部高光 + 投影）、文字阴影。
     统一走「参数 → 预渲染贴图 → 缓存 → blit」，颜色进缓存 key，切主题自动换一套；
  3. **配色补齐**：`theme.py` 的 `Palette` 加了 17 个字段（渐变两端色、柔光色、栏面色
     与分隔线、棋盘面板三色、卡片三色、顶部高光、投影参数、按钮三段色、强调色），
     日夜各一套，旧字段一个没删；
  4. **逐个套上去**：背景→渐变+柔光；棋盘→圆角面板（渐变+细描边+投影）；顶栏底栏→
     半透明玻璃 + 1 像素分隔线 + 朝盘面一侧的渐隐阴影；按钮→渐变+描边+投影，悬停提亮
     描边、按下下沉 2 像素并收起投影；菜单项→悬停加圆角高亮 + 左侧强调条；滑杆→凹槽
     轨道 + 上下双色的已填充段；拨杆滑块、金币、红心都补了一层投影；
  5. **顺带修掉三处排版事故**（都是画出来才看见的）：开始页的规则文字比卡片还宽、
     直接压在卡片边框上；选关页下半屏空了一大片；菜单最后一项正好顶在面板底边上。
- **实际效果**：界面从「纯色底 + 硬描边」变成有层次的卡片式界面；`tools/perf_bench.py`
  量得各界面整帧 2.45~5.72 ms（最贵的是菜单展开，要整屏铺一层半透明遮罩），
  远在 60 FPS 的 16.7 ms 预算之内。新增 `tests/test_paint.py`（34 个用例），
  全量 227 个用例通过。
- **人工修改**：
  - **没有直接换配色**：先看截图定位到「缺层次」，才决定做渐变 + 投影 + 高光，
    配色只是在原有基础上补齐了几组「成对」的色（深色端/浅色端、上色/下色）；
  - 大图**统一按 2 倍画再缩回来**，圆角与描边就自带抗锯齿了 —— 比给每个圆角单独
    做抗锯齿省事得多；缩小时透明处必须填成填充色，否则边缘渗黑（这个坑在记录 G
    做圆点时已经踩过一次）；
  - 滑杆的已填充段**故意不走去缓存的卡片贴图**：拖动时宽度每帧都变，缓存会被灌满
    然后整片清空，反倒比直接画两个圆角矩形更费 —— 这是自己权衡后改掉的；
  - 修菜单留白时没有直接拍一个更大的高度，而是把「项数 × 52 + 标题区 + 底部留白」
    算成了公式，并把这个关系写进测试断言（`menu.rect.bottom - last.bottom >= 18`）；
  - 写「线宽处处相等」这类断言时踩过一次自己量错行的坑（见记录 H），所以这次所有
    像素断言都先打印实际值确认，再写死阈值。

---

### 记录 J：开始页四个入口与字母玩法 —— 把「字模」当成「关卡遮罩」

- **工具**：DeepSeek
- **提出的要求**：美化开始页 —— 标题「一箭又一箭」做成艺术字，下面放四个按钮：
  规则介绍 / 基础玩法 / 字母玩法 / 随机关卡。规则页要写清按键功能（比如 `U` 是撤销），
  但**要去掉「按 `N` 是随机关卡」这一条**；字母玩法共 26 关，分别是 26 个字母形状的
  关卡；随机关卡就是把原来按 `N` 触发的能力挪到按钮上。
- **AI 完成内容**：
  1. **艺术字不靠字体，靠后处理**：`paint.art_text()` 先用普通字体渲染一遍文字，再把
     整张图放大 `ART_SUPERSAMPLE=3` 倍，按「圆盘偏移」在四周回填一遍做出**等厚度描边**
     （用 `BLEND_RGBA_MAX` 取亮，避免描边自相交处出现暗缝），然后把竖向渐变与顶部高光
     按 `BLEND_RGBA_MULT` 乘到 alpha 上，最后缩回原尺寸。这样「描边 + 渐变 + 高光 + 投影」
     一次到位，而且**不必给项目塞一个艺术字体文件**；
  2. **字母 = 遮罩**：本项目的关卡生成器本来就支持「在一张遮罩里铺箭」（造型 / 矩形），
     所以「字母关」不需要新玩法，只需要一张字母形状的遮罩。`game/letters.py` 内置 26 个
     5×7 点阵字模，`letter_cells()` 把它们按 `scale=2`、`margin=1` 光栅化成 16 行 × 12 列
     的格子集合，直接喂给现成的生成器；
  3. **连通性必须开八连通**：字母的斜边（`C` 的弧、`S` 的腰、`J`/`Q` 的尾）在 5×7 点阵里
     是**对角相接**的，用原来的四连通判定会把这些笔画判成断开，生成器随即拒绝；
     所以 `is_connected(cells, diagonal=True)` 只对字母关开八连通；
  4. **难度沿字母序递进**：`tools/generate_letters.py` 的 `plan_for(index)` 让
     `ray_pref` 从 0.45 爬到 0.90、开局可飞上限从 0.58 收到 0.30，
     再套用基础关那套「逆向构造 + 校验（连通 / 解可走 / 有阻挡）」；
  5. **四个入口 + 两个选关页**：状态机从 6 个状态扩到 8 个（开始 / 规则 / 基础选关 /
     字母选关 / 对局 / 过关 / 失败 / 全清）。`main.py` 用 `self.track`（`"basic"` /
     `"letter"`）记住当前在走哪条线、`self.levels` 指向对应的关卡列表；
     存档判重沿用原来的 `current_level is levels[level_index]` —— 随机关卡不属于任何列表、
     字母关 id 是 `"A"`~`"Z"`，两者都**天然不会和基础关的 1~12 撞车**，一行都不用改；
  6. **`N` 键彻底退场**：`_handle_key` 删掉 `K_n` 分支，规则页的快捷键表里也去掉那一行 ——
     功能搬到按钮上之后，留着这个键只会让规则页说不清楚。
- **实际效果**：开始页从「两个按钮」变成「艺术字标题 + 四个入口 + 进度统计卡」；
  26 个字母关全部生成成功（平均 12.1 支箭、填充率 0.998、开局可飞 37%、被挡 63%）。
  新增 `tests/test_letters.py`（104 个用例），全量 332 个用例通过；新增界面的整帧耗时
  1.70~6.66 ms（最贵的规则页也远在 16.7 ms 预算内）。截图重出 20 张，逐张人工核对。
- **人工修改**：
  - **先写探针脚本再动主流程**：26 个字母能不能生成、遮罩连不连通、盘面画出来像不像那个
    字母，都是先用临时脚本一次性验完（还顺手把 A/M/W/S/I 的盘面渲染出来肉眼比了一遍），
    确认没问题才去改状态机和 `main.py`；
  - **踩过一次「行列写反」**：`board_size()` 一开始返回 `(列, 行)`，而调用方按 `(行, 列)`
    解包，结果遮罩被裁掉一块，`C` / `G` / `J` / `U` / `V` 五个字母直接生成失败。
    查清楚之后除了改回来，还**专门补了一条 `test_board_size_is_rows_then_cols`** ——
    这种「顺序写反但两边都能跑」的错误，只有测试能兜住；
  - **不许为了字母玩法新加一套机制**：宁可让点阵字模去迁就现有的「遮罩 + 逆向构造」，
    也不给字母关单开一条生成路径 —— 这样 26 关自动继承了基础关已验证过的可通关性与阻挡；
  - 写规则页时把「按键功能」和「玩法说明」分成两栏排版，并让快捷键表直接从代码里的
    `KEY_HINTS` 生成，**改了键位不会漏改文档**。

### 记录 K：开始页的暗纹底与卡通箭吉祥物 —— 三个「只表现为画错了」的坑

- **工具**：DeepSeek
- **提出的要求**：玩家给了一张开始页设计稿，提三件事 —— ①「我说的艺术字是截图里面
  标题下方那个箭」；②「开始界面的背景也换一下，换成跟截图里面类似的（但要有一定改动）」；
  ③「按键配色也是」。
- **AI 完成内容**：先逐像素采样参考图定位风格（薄荷青底 + 同色箭形暗纹 + 亮蓝按钮 +
  标题下方一只带脸的蓝色卡通箭），再分三块实现：
  1. `paint.pattern_layer()` / `blit_pattern()`：底色渐变 + 平铺的箭形暗纹合成成
     **一张不透明贴图**；图案复用游戏自己那支箭（`_arrow_shape`），隔行错开半格、
     奇偶格上下翻转；`Palette` 补 `menu_top/menu_bottom/pattern/pattern_alpha`；
  2. `paint.arrow_mascot()`：投影 / 外描边 / 渐变箭身 / 贴边暗面 / 高光块 + 四角星 /
     一张脸（眼白、瞳孔、高光点、两道眉毛、一道笑），整体倾斜 10° 再缩回；
     新增 `_round_polygon()`（正规圆角多边形）与 `_mascot_face()`；
  3. 主按钮改成「亮蓝填充 + **比填充更深**的描边」，开始页整体重排给吉祥物让位。
- **实际效果**：开始页变成「薄荷青（日间）/ 深蓝（夜间）+ 同色箭形暗纹 + 贴纸标题 +
  卡通箭吉祥物 + 蓝白交替的按钮」；吉祥物占屏宽 49%，夹在副标题与按钮之间互不重叠；
  新增 2 条用例、改 3 条，全量 354 个用例通过；开始页中位 **3.02 ms**（比上一版 3.38
  还低 —— 整块底预合成成一张贴图，一帧只剩一次 blit）。
- **人工修改**：
  - **第一版画出来完全不像箭**，于是把每一层单独 dump 出来看，连查三步：
    ① 剪影本身像一根**骨头** —— 圆角是「在三个顶点各画一个圆」，圆会鼓到边的外侧去。
    改成正规圆角多边形：角平分线内缩 `r / tan(θ/2)` 得切点、圆心落在距两边都是 `r`
    的地方，先填切点围成的多边形再补角圆；切点还必须按绕行顺序连（先出边、后进边），
    顺序错了会连成蝴蝶结；
    ② **箭尾被烧黑** —— 渐变只铺在「箭身框」内，箭尾的圆头伸到框外，那一块被
    `BLEND_RGBA_MULT` 乘成全透明，底下的深色描边就露出来了。渐变改成铺满整张画布
    （框内保持斜坡、框外向上取最亮、向下取最深）。这个 bug 只影响箭尾，很容易被
    当成「设计如此」，所以专门写了一条断言盯它（箭尾最左 16 px 处必须是蓝色箭身）；
    ③ **五官浮在箭身外面** —— 眼睛原先是直接画在画布上的，位置一偏就跑出箭身。
    改成五官单独成层，再用箭身剪影做一次 `BLEND_RGBA_MULT` 裁一刀（剪影是纯白全不
    透明，乘上去只影响 alpha，等于按剪影裁剪）；
  - **排位置按「画出来的尺寸」而不是传进去的宽度**：吉祥物的 `width` 是旋转前的箭身
    宽，倾斜 10° 之后包围盒膨胀约 28%，拿它直接排会与副标题、按钮叠在一起。整页因此
    重排过一次（标题 `168`、副标题 `246`、吉祥物中心 `386`、四个入口
    `552/642/732/822`、进度卡 `880~1056`），并把「副标题底 < 吉祥物顶 < 第一个按钮顶」
    写成断言；
  - **配色取「同一套结构换底色」而不是照抄**：参考图的背景是薄荷青，直接照搬会让夜间
    主题变得刺眼，所以夜间只加暗纹、底色仍是原来的深蓝；暗纹的透明度也按底色分别定
    （日间 62 压深、夜间 26 提亮）。
- **人为改写**：无（三处几何与裁剪的修法都是先 dump 中间层、看清是哪一层画多了之后
  再改的，没有靠猜）。

---

## 复查提醒（助教可能问到的点）

- `can_fly_arrow` 为什么只看箭头射线、不需要检查身体路径？（因为身体沿自身折线滑出，不横扫）
- 逆向构造法为什么**只需要一条**约束？（这条约束等价于「按放箭的倒序点，每一步射线都是空的」；
  多加的「身体不许压在别人射线上」会让任意两支箭互不接触，开局全盘都能飞，玩法就没了）
- 生成出来的关卡开局有几支箭能直接飞？（第 1 关 8/12，第 12 关 2/19；逐关递减）
- 求解器为什么用拓扑排序而不是深搜？（一支箭能不能飞只取决于谁挡它，清掉箭不影响别人，
  所以问题就是有向图的拓扑排序；深搜在有阻挡的盘面上会指数爆炸，把有解的关卡判成无解）
- 什么样的关卡算无解？（「谁挡谁」的图里存在环，环里的箭互相封死，谁都走不了第一步）
- 为什么启动时要按屏幕把窗口收敛一遍？（进程非 DPI 感知，窗口一旦高过桌面，
  Windows 会把它垂直居中，标题栏与底边双双跑到屏幕外，就抓不到边框也拖不动）
- 点击为什么改成松手才生效？（为了把同一个左键分给「点击线段」和「拖动棋盘」两件事：
  按下记账、抬起了结，位移不超过 8 逻辑像素才算点击，超过就改判为拖动、这次点击作废。
  填充率 0.94~1.00 的盘面上几乎每格都是线段，按下即点的话想拖棋盘必定先点飞一支箭）
- 棋盘为什么拖不出屏幕？（缩放/拖动都会过一遍夹取：装得下就锁死在居中，装不下则最多
  拖到棋盘边缘与可视区边缘对齐；夹取结果立刻写回 `pan`，否则拖到边界后往回拖会先空转）
- 飞出动画为什么要专门取折线上的一段，不能直接每格取一个点？（两点之间隔着整整一格，
  落在中间的拐角会被连成一条斜弦切掉，直角变斜角；`offset` 连续增长时每秒「削平复原」
  26 次，看起来就是转弯处在抖）
- 为什么抗锯齿只做圆点和箭头，不做整条线？（折线都是横平竖直的，直段的边本来就是锐利的
  整数像素边界，不需要抗锯齿；整幅超采样每帧要新建上百万像素的 Surface 再缩回来，
  约 2.5 ms/帧，而只给两处小贴图做超采样可以忽略不计）
- 为什么直线段中间的节点不补圆点？（每个节点都补会在线上挂一串小疙瘩、直线看着不直；
  只在两端与真正拐弯的节点补。那个「鼓出 1 像素」的根因后来在记录 H 里查清了：
  圆点直径取成了 `2r+1`，比线宽多 1 个像素）
- 为什么单帧步进要夹上限？（切窗口回来 `clock.tick()` 可能一次返回几百毫秒，
  飞行线一步跨过好几格就是「瞬移」；夹到 50 ms 后宁可慢半拍也不跳）
- 线宽为什么取格距的 0.30？（对着参考录屏抽帧量的：金黄那支箭实占 14 px、格心间距约
  56 px，线宽约 1/4 格；上限 15 px 是为了大格距下不把点阵糊掉）
- 箭头为什么做成 4 个顶点的「飞镖」而不是三角？（参考图里的箭头后缘是往内兜的，
  把线身的圆头包进凹口，箭头屁股后面才不露出一截；两枚倒钩前掠 0.42 倍线宽）
- 描边为什么用「乘系数」而不是「减常数」？（乘系数色相不变；减常数会把蓝、紫这类
  深色直接压成黑边，浅色底（日间）还得把系数调轻，所以做成主题参数）
- 描边为什么不是「把整支箭放大一圈」？（放大对线身成立，但箭头的斜边会跟着一起变粗，
  箭头看着像包了条毛巾；正确做法是线身加宽 `2×rim`、箭头往外撑 `rim`，两边才是等厚的一圈）
- 圆点直径为什么必须严格等于线宽？（pygame 画宽 `w` 的线涂的是 `y_center-(w-1)//2`
  起的 `w` 行，圆点若按画整数圆的老规矩取 `2r+1`，就比线宽多 1 像素，每个拐点、
  每个端头都会鼓出一小块 —— 这就是放大了看「线上挂着一串疙瘩」的真正原因）
- 界面「美化」具体改了什么？（背景换成竖向渐变 + 一团柔光；棋盘垫在圆角面板上；
  顶栏底栏做成半透明玻璃 + 渐隐阴影；按钮、卡片、菜单项、滑杆全部改成渐变填充 +
  细描边 + 投影；按钮悬停提亮描边、按下下沉 2 像素并收起投影）
- 为什么这些效果在 pygame 里要「预渲染成贴图」？（pygame 画不出柔边，渐变、柔光、
  模糊投影都得先画好再贴；所以统一走「参数 → 贴图 → 缓存 → blit」，一帧只是几次
  blit。缓存的 key 里包含全部颜色，切主题会自动换一套，不需要手动清）
- 大图为什么要按 2 倍画再缩回来？（圆角、斜边、描边一次就都有抗锯齿了，比给每个
  圆角单独做抗锯齿省事。缩小时透明处要填成填充色，否则边缘会渗黑）
- 缓存有没有上限？滑杆为什么不走缓存？（有上限，超了就整片清空；滑杆已填充的那一段
  宽度每帧都在变，走缓存会被不停灌满然后清空，反倒比直接画两个圆角矩形更费）
- 加了这么多贴图，帧率还够吗？（`tools/perf_bench.py` 量的稳态：开始页 3.52 ms、
  选关页 4.33 ms、第 1 关 2.45 ms、第 12 关 3.44 ms、飞行中 3.39 ms、菜单展开 5.72 ms，
  都在 16.7 ms 的预算之内）
- 怎么证明「美化」没有把排版或交互改坏？（`tests/test_paint.py` 34 个用例：像素级的
  渐变 / 柔光 / 投影 / 卡片断言，按钮三态必须互不相同且按下下沉 2 像素，两套主题字段
  齐全，8 个界面 × 2 套主题整页渲染且颜色数 > 6，菜单最后一项离底边 ≥ 18 像素，
  以及「改了菜单项间距后点击仍然命中正确的项」）
- 艺术字是怎么做出来的，要额外字体文件吗？（不要。先按普通字体渲染，再放大 3 倍、
  按「圆盘偏移」四周回填做出等厚描边，然后把渐变和高光按 alpha 乘上去，最后缩回原尺寸）
- 字母关和基础关是两套生成器吗？（不是。字母关只是换了一张「遮罩」——26 个 5×7 点阵字模
  光栅化成格子集合，喂给同一个逆向构造生成器，所以自动继承了已验证的可通关性与阻挡）
- 为什么字母关的连通性要用八连通？（字母的斜边，比如 `C` 的弧、`S` 的腰、`J`/`Q` 的尾，
  在点阵里是对角相接的，四连通会把它们判成断开、生成器直接拒绝）
- 随机关卡为什么不会污染闯关进度？（存档判重用的是 `current_level is levels[level_index]`，
  随机关卡不属于任何列表、也不在选关列表里，所以永远不满足这个条件）
- 字母关的 id 会不会和基础关撞？（不会，字母关 id 是 `"A"`~`"Z"` 字符串，基础关是 1~12 整数）
- 为什么要把 `N` 键删掉？（随机关卡已经搬到开始页的按钮上，功能重复的键留在规则页里
  只会让人以为还有别的入口；所以按键处理与规则页的快捷键表一起删）
- 26 个字母关的难度是怎么排的？（按字母序递进：`ray_pref` 从 0.45 爬到 0.90，
  开局可飞上限从 0.58 收到 0.30，和基础关同一套旋钮）
- 开始页那只卡通箭是怎么来的，要美术资源吗？（不要，纯几何拼的：投影 → 外描边 →
  渐变箭身 → 贴边暗面（剪影减去「往左上挪几像素的剪影」）→ 高光块与四角星 → 一张脸，
  最后整体旋转缩回。切主题会自动换配色）
- 箭头那三个圆角为什么不能用「在顶点画圆」？（圆会鼓到边的外侧，锐角三角形画出来
  像一根骨头。正规做法是角平分线内缩 `r / tan(θ/2)` 取切点、圆心距两边都是 `r`，
  先填切点多边形再补角圆 —— 而切点还得按绕行顺序连，顺序错了会连成蝴蝶结）
- 吉祥物的五官为什么不直接画在画布上？（位置一偏就浮在箭身外面。五官单独成层之后用
  剪影做一次 `BLEND_RGBA_MULT`，等于按剪影裁剪 —— 剪影是纯白全不透明的，乘上去
  只影响 alpha，不会改颜色）
- 开始页背景的暗纹是怎么铺的？（把底色渐变与平铺的箭形暗纹先合成成**一张不透明贴图**
  再整屏 blit；图案就用游戏自己那支箭，隔行错开半格、奇偶格上下翻转。若不预合成，
  光平铺就是上百次小 blit 外加整屏透明混合）
- 参考图的背景是薄荷青，夜间主题怎么处理？（配色不照抄：夜间只加同色暗纹、底色留在
  深蓝上；暗纹透明度按底色分别定，日间 62 压深、夜间 26 提亮）
